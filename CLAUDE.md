# Claude Code Behavior

## Task Tracking

Before doing any task (especially multi-step ones), use the `task_write` tool to create a task list.

After completing each task, use `task_update` or `task_complete` to mark it as done.

When finished with all work, use `task_check` to verify everything is complete.